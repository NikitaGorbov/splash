// Read a word from input source and add it to a char array. Set end of line
// flag if the word ended with '\n'. Return the created array.
char *get_word(int *endOfLineFlag);

// Read words until end of line flag is set. Add words to a char* array and
// return this array.
char **get_list();
